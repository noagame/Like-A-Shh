import { createClient } from "@/lib/supabase/server";
import Link from "next/link";
import Image from "next/image";

type Post = { id: string; title: string; slug: string; excerpt: string; cover_image_url: string | null; published_at: string };
export default async function BlogPage() { const supabase = await createClient(); const { data } = await supabase.from("blog_posts").select("id,title,slug,excerpt,cover_image_url,published_at").eq("status", "published").order("published_at", { ascending: false }); const posts = (data ?? []) as Post[];
  return <main className="min-h-screen bg-[#09090b] px-4 py-24 text-white sm:px-6"><section className="mx-auto max-w-6xl"><p className="text-xs uppercase tracking-[0.25em] text-[#48CAE4]">Like a Shh</p><h1 className="mt-2 text-4xl font-bold sm:text-5xl" style={{ fontFamily: "var(--font-serif)" }}>Blog & bienestar</h1><p className="mt-3 max-w-2xl text-white/60">Ideas, historias y recursos para acompañar tu práctica.</p><div className="mt-10 grid gap-5 sm:grid-cols-2 lg:grid-cols-3">{posts.map((post) => <article key={post.id} className="overflow-hidden rounded-2xl border border-white/10 bg-white/5 backdrop-blur-xl">{post.cover_image_url && <Image src={post.cover_image_url} alt="" width={720} height={400} className="h-40 w-full object-cover" sizes="(max-width: 640px) 100vw, (max-width: 1024px) 50vw, 33vw" />}<div className="p-5"><time className="text-[11px] text-[#D4AF37]">{new Date(post.published_at).toLocaleDateString("es-CL", { dateStyle: "long" })}</time><h2 className="mt-2 text-xl font-bold">{post.title}</h2><p className="mt-2 text-sm leading-relaxed text-white/60">{post.excerpt}</p><Link className="mt-4 inline-block text-sm font-semibold text-[#48CAE4] hover:underline" href={`/blog/${post.slug}`}>Leer artículo →</Link></div></article>)}</div>{posts.length === 0 && <p className="mt-10 text-white/50">Pronto publicaremos nuevos artículos.</p>}</section></main>; }
